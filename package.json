{
  "name": "emotion-detection",
  "version": "1.0.0",
  "description": "a simple program which, with a given string, will tell us if it’s mood is sad or happy",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hybeecodes",
  "license": "ISC",
  "dependencies": {
    "brain.js": "^1.6.1"
  }
}
